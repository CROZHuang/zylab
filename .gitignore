__pycache__/
*.pyc
.zylab/map/
dist/
.zylab-home/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/.zylab-home/graft/zylab-1a743eb06b5af59dc1da/

# 一次性采集日志，不入库
logs/
# CI 里 tee 出来的临时日志（ci_annotate.py 读它），不入库
unit.log
parity.log
